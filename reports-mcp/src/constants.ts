export const API_BASE_URL = process.env.API_BASE_URL || "http://localhost:3001/api";
export const API_KEY = process.env.API_KEY || "laapak-api-key-2024";
export const JWT_TOKEN = process.env.JWT_TOKEN || "";

// Maximum response size in characters to prevent overwhelming the context window
export const CHARACTER_LIMIT = 25000;
